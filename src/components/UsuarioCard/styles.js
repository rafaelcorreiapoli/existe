import { ACCENT } from '../../resources/colors'
const styles = {
  row: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  },
  title: {
    width: '100%',
    whiteSpace: 'nowrap',
    overflow: 'hidden',
    textOverflow: 'ellipsis'
  },
}

export default styles;
