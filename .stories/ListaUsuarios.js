import React from 'react';

import { storiesOf, action } from '@kadira/storybook';

storiesOf('Button', module)
  .add('with text', () => (
    <ListaUsuarios />
  ))
  .add('with some emoji', () => (
    <ListaUsuarios />
  ));
